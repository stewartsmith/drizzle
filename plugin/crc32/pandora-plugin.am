## Generated by register_plugins.py
EXTRA_DIST += plugin/crc32/pandora-plugin.ac

${top_srcdir}/plugin/crc32/pandora-plugin.am: ${top_srcdir}/config/register_plugins.py plugin/crc32/.plugin.ini.stamp

${top_srcdir}/plugin/crc32/pandora-plugin.ac: ${top_srcdir}/config/register_plugins.py plugin/crc32/.plugin.ini.stamp

configure: ${top_srcdir}/plugin/crc32/pandora-plugin.ac

# Prevent errors when a plugin dir is removed
plugin/crc32/plugin.ini:

# Failures to update the plugin.ini are ignored to permit plugins to be deleted
# cleanly.
${top_srcdir}/plugin/crc32/.plugin.ini.stamp: plugin/crc32/plugin.ini
	@if [ ! -e ${top_srcdir}/plugin/crc32/plugin.ini ]; then \
	    echo "plugin/crc32/plugin.ini is missing"; \
	else \
	    cmp -s $< $@; \
	    if [ $$? -ne 0 ]; then \
	      echo 'cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/crc32'; \
	      unchanged=`cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/crc32` ;\
	      if [ $$? -ne 0 ]; then \
	        echo "**** register_plugins failed ****"; \
	        false; \
	      fi && \
	      for plugin_dir in $$unchanged; do \
	        echo "plugin $$plugin_dir unchanged." ; \
	        touch -r $$plugin_dir/pandora-plugin.am $$plugin_dir/.plugin.ini.stamp; \
	      done && \
              cp -a $< $@ || echo "Failed to update $@"; \
	    fi; \
	fi

plugin_libcrc32_dir=${top_srcdir}/plugin/crc32
EXTRA_DIST += plugin/crc32/plugin.ini
if BUILD_CRC32_PLUGIN
  noinst_LTLIBRARIES+=plugin/libcrc32_plugin.la
  plugin_libcrc32_plugin_la_LIBADD=
  plugin_libcrc32_plugin_la_DEPENDENCIES=
  plugin_libcrc32_plugin_la_LDFLAGS=$(AM_LDFLAGS) 
  plugin_libcrc32_plugin_la_CPPFLAGS=$(AM_CPPFLAGS) 
  plugin_libcrc32_plugin_la_CXXFLAGS=$(AM_CXXFLAGS) 
  plugin_libcrc32_plugin_la_CFLAGS=$(AM_CFLAGS) 

  plugin_libcrc32_plugin_la_SOURCES= plugin/crc32/crc32udf.cc
  PANDORA_DYNAMIC_LDADDS+=${top_builddir}/plugin/libcrc32_plugin.la
endif
