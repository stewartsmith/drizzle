## Generated by register_plugins.py
EXTRA_DIST += plugin/console/pandora-plugin.ac

${top_srcdir}/plugin/console/pandora-plugin.am: ${top_srcdir}/config/register_plugins.py plugin/console/.plugin.ini.stamp

${top_srcdir}/plugin/console/pandora-plugin.ac: ${top_srcdir}/config/register_plugins.py plugin/console/.plugin.ini.stamp

configure: ${top_srcdir}/plugin/console/pandora-plugin.ac

# Prevent errors when a plugin dir is removed
plugin/console/plugin.ini:

# Failures to update the plugin.ini are ignored to permit plugins to be deleted
# cleanly.
${top_srcdir}/plugin/console/.plugin.ini.stamp: plugin/console/plugin.ini
	@if [ ! -e ${top_srcdir}/plugin/console/plugin.ini ]; then \
	    echo "plugin/console/plugin.ini is missing"; \
	else \
	    cmp -s $< $@; \
	    if [ $$? -ne 0 ]; then \
	      echo 'cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/console'; \
	      unchanged=`cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/console` ;\
	      if [ $$? -ne 0 ]; then \
	        echo "**** register_plugins failed ****"; \
	        false; \
	      fi && \
	      for plugin_dir in $$unchanged; do \
	        echo "plugin $$plugin_dir unchanged." ; \
	        touch -r $$plugin_dir/pandora-plugin.am $$plugin_dir/.plugin.ini.stamp; \
	      done && \
              cp -a $< $@ || echo "Failed to update $@"; \
	    fi; \
	fi

plugin_libconsole_dir=${top_srcdir}/plugin/console
EXTRA_DIST += plugin/console/plugin.ini
if BUILD_CONSOLE_PLUGIN
  noinst_LTLIBRARIES+=plugin/libconsole_plugin.la
  plugin_libconsole_plugin_la_LIBADD=
  plugin_libconsole_plugin_la_DEPENDENCIES=
  plugin_libconsole_plugin_la_LDFLAGS=$(AM_LDFLAGS) 
  plugin_libconsole_plugin_la_CPPFLAGS=$(AM_CPPFLAGS) 
  plugin_libconsole_plugin_la_CXXFLAGS=$(AM_CXXFLAGS) 
  plugin_libconsole_plugin_la_CFLAGS=$(AM_CFLAGS) 

  plugin_libconsole_plugin_la_SOURCES= plugin/console/console.cc
  PANDORA_DYNAMIC_LDADDS+=${top_builddir}/plugin/libconsole_plugin.la
endif
