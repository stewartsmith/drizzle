## Generated by register_plugins.py
EXTRA_DIST += plugin/md5/pandora-plugin.ac

${top_srcdir}/plugin/md5/pandora-plugin.am: ${top_srcdir}/config/register_plugins.py plugin/md5/.plugin.ini.stamp

${top_srcdir}/plugin/md5/pandora-plugin.ac: ${top_srcdir}/config/register_plugins.py plugin/md5/.plugin.ini.stamp

configure: ${top_srcdir}/plugin/md5/pandora-plugin.ac

# Prevent errors when a plugin dir is removed
plugin/md5/plugin.ini:

# Failures to update the plugin.ini are ignored to permit plugins to be deleted
# cleanly.
${top_srcdir}/plugin/md5/.plugin.ini.stamp: plugin/md5/plugin.ini
	@if [ ! -e ${top_srcdir}/plugin/md5/plugin.ini ]; then \
	    echo "plugin/md5/plugin.ini is missing"; \
	else \
	    cmp -s $< $@; \
	    if [ $$? -ne 0 ]; then \
	      echo 'cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/md5'; \
	      unchanged=`cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/md5` ;\
	      if [ $$? -ne 0 ]; then \
	        echo "**** register_plugins failed ****"; \
	        false; \
	      fi && \
	      for plugin_dir in $$unchanged; do \
	        echo "plugin $$plugin_dir unchanged." ; \
	        touch -r $$plugin_dir/pandora-plugin.am $$plugin_dir/.plugin.ini.stamp; \
	      done && \
              cp -a $< $@ || echo "Failed to update $@"; \
	    fi; \
	fi

plugin_libmd5_dir=${top_srcdir}/plugin/md5
EXTRA_DIST += plugin/md5/plugin.ini
if BUILD_MD5_PLUGIN
  noinst_LTLIBRARIES+=plugin/libmd5_plugin.la
  plugin_libmd5_plugin_la_LIBADD=
  plugin_libmd5_plugin_la_DEPENDENCIES=
  plugin_libmd5_plugin_la_LDFLAGS=$(AM_LDFLAGS) ${LTMD5PLUGINDEP}
  plugin_libmd5_plugin_la_CPPFLAGS=$(AM_CPPFLAGS) 
  plugin_libmd5_plugin_la_CXXFLAGS=$(AM_CXXFLAGS) 
  plugin_libmd5_plugin_la_CFLAGS=$(AM_CFLAGS) 

  plugin_libmd5_plugin_la_SOURCES= plugin/md5/md5.cc
  PANDORA_DYNAMIC_LDADDS+=${top_builddir}/plugin/libmd5_plugin.la
endif
