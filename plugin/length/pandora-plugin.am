## Generated by register_plugins.py
EXTRA_DIST += plugin/length/pandora-plugin.ac

${top_srcdir}/plugin/length/pandora-plugin.am: ${top_srcdir}/config/register_plugins.py plugin/length/.plugin.ini.stamp

${top_srcdir}/plugin/length/pandora-plugin.ac: ${top_srcdir}/config/register_plugins.py plugin/length/.plugin.ini.stamp

configure: ${top_srcdir}/plugin/length/pandora-plugin.ac

# Prevent errors when a plugin dir is removed
plugin/length/plugin.ini:

# Failures to update the plugin.ini are ignored to permit plugins to be deleted
# cleanly.
${top_srcdir}/plugin/length/.plugin.ini.stamp: plugin/length/plugin.ini
	@if [ ! -e ${top_srcdir}/plugin/length/plugin.ini ]; then \
	    echo "plugin/length/plugin.ini is missing"; \
	else \
	    cmp -s $< $@; \
	    if [ $$? -ne 0 ]; then \
	      echo 'cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/length'; \
	      unchanged=`cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/length` ;\
	      if [ $$? -ne 0 ]; then \
	        echo "**** register_plugins failed ****"; \
	        false; \
	      fi && \
	      for plugin_dir in $$unchanged; do \
	        echo "plugin $$plugin_dir unchanged." ; \
	        touch -r $$plugin_dir/pandora-plugin.am $$plugin_dir/.plugin.ini.stamp; \
	      done && \
              cp -a $< $@ || echo "Failed to update $@"; \
	    fi; \
	fi

plugin_liblength_dir=${top_srcdir}/plugin/length
EXTRA_DIST += plugin/length/plugin.ini
if BUILD_LENGTH_PLUGIN
  noinst_LTLIBRARIES+=plugin/liblength_plugin.la
  plugin_liblength_plugin_la_LIBADD=
  plugin_liblength_plugin_la_DEPENDENCIES=
  plugin_liblength_plugin_la_LDFLAGS=$(AM_LDFLAGS) 
  plugin_liblength_plugin_la_CPPFLAGS=$(AM_CPPFLAGS) 
  plugin_liblength_plugin_la_CXXFLAGS=$(AM_CXXFLAGS) 
  plugin_liblength_plugin_la_CFLAGS=$(AM_CFLAGS) 

  plugin_liblength_plugin_la_SOURCES= plugin/length/length.cc
  PANDORA_DYNAMIC_LDADDS+=${top_builddir}/plugin/liblength_plugin.la
endif
