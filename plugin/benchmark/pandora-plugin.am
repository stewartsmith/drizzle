## Generated by register_plugins.py
EXTRA_DIST += plugin/benchmark/pandora-plugin.ac

${top_srcdir}/plugin/benchmark/pandora-plugin.am: ${top_srcdir}/config/register_plugins.py plugin/benchmark/.plugin.ini.stamp

${top_srcdir}/plugin/benchmark/pandora-plugin.ac: ${top_srcdir}/config/register_plugins.py plugin/benchmark/.plugin.ini.stamp

configure: ${top_srcdir}/plugin/benchmark/pandora-plugin.ac

# Prevent errors when a plugin dir is removed
plugin/benchmark/plugin.ini:

# Failures to update the plugin.ini are ignored to permit plugins to be deleted
# cleanly.
${top_srcdir}/plugin/benchmark/.plugin.ini.stamp: plugin/benchmark/plugin.ini
	@if [ ! -e ${top_srcdir}/plugin/benchmark/plugin.ini ]; then \
	    echo "plugin/benchmark/plugin.ini is missing"; \
	else \
	    cmp -s $< $@; \
	    if [ $$? -ne 0 ]; then \
	      echo 'cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/benchmark'; \
	      unchanged=`cd ${srcdir} && python config/register_plugins.py ${top_srcdir} plugin/benchmark` ;\
	      if [ $$? -ne 0 ]; then \
	        echo "**** register_plugins failed ****"; \
	        false; \
	      fi && \
	      for plugin_dir in $$unchanged; do \
	        echo "plugin $$plugin_dir unchanged." ; \
	        touch -r $$plugin_dir/pandora-plugin.am $$plugin_dir/.plugin.ini.stamp; \
	      done && \
              cp -a $< $@ || echo "Failed to update $@"; \
	    fi; \
	fi

plugin_libbenchmark_dir=${top_srcdir}/plugin/benchmark
EXTRA_DIST += plugin/benchmark/plugin.ini
if BUILD_BENCHMARK_PLUGIN
  noinst_LTLIBRARIES+=plugin/libbenchmark_plugin.la
  plugin_libbenchmark_plugin_la_LIBADD=
  plugin_libbenchmark_plugin_la_DEPENDENCIES=
  plugin_libbenchmark_plugin_la_LDFLAGS=$(AM_LDFLAGS) 
  plugin_libbenchmark_plugin_la_CPPFLAGS=$(AM_CPPFLAGS) 
  plugin_libbenchmark_plugin_la_CXXFLAGS=$(AM_CXXFLAGS) 
  plugin_libbenchmark_plugin_la_CFLAGS=$(AM_CFLAGS) 

  plugin_libbenchmark_plugin_la_SOURCES= plugin/benchmark/benchmarkudf.cc
  PANDORA_DYNAMIC_LDADDS+=${top_builddir}/plugin/libbenchmark_plugin.la
endif
