vcol_csv                    : when altering a table outparam->file is 0x0 in procedure open_table_from_share
vcol_archive                : Lost connection to server when altering a table by adding a new (even non-virtual) column
rpl_vcol                    : Sync with master not working
