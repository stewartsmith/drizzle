#
# Tests the INFORMATION_SCHEMA views which come with the transaction
# log
#

# Populate log with some records...
--source suite/transaction_log/t/insert.inc

# Query the INFORMATION_SCHEMA views and verify
# that the information contained in them matches
# the transaction log.

--replace_column 7 X 8 X
SELECT * FROM INFORMATION_SCHEMA.TRANSACTION_LOG;
--replace_column 1 X
SELECT * FROM INFORMATION_SCHEMA.TRANSACTION_LOG_ENTRIES;
--replace_column 1 X 4 X 5 X
SELECT * FROM INFORMATION_SCHEMA.TRANSACTION_LOG_TRANSACTIONS;

# Truncate the log file to reset for the next test
--source suite/transaction_log/t/truncate_log.inc
